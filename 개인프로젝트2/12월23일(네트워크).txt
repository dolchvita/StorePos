
네트워크 연동하기

서버소켓을 만들어서 pory에 해당하는 접속을 만들었다
--> 무한대기상태 빠지기 주의!
서버소켓을 작동시키는 전용 쓰레드가 있어야 함!


admin에서 메시지를 보낼 때 작동을 멈춤

일단 쓰레드를 따로 만들어서 관리해보자
공통의 쓰레드를 만들면 좋겠군



- 모든 클라이언트의 화면에 글씨를 출력하려면?

// 서버는 리슨에서 샌드 메서드에게 읽은 메시지를 전달해줄 의무가 있다!

1) 
Vector로 저장되어 있는 모든 소켓들에게 전달
중요한 것은 서버의 역할!! 클라이언트에게 메세지를 뿌리는 것도
서버에서 들은 것(listen)을 쓸 수 있게 전달(sendMag)해야 했던 것


글자색 바꾸기 함수
setForeground

상위객체가 매개변수를 가지면
그 객체를 상속받는 객체 또한 매개변수로 받아야 한다















