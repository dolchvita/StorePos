라벨을 탄생시키고 그 안에 아이콘 넣기

> 포스페이지 (SubMain)
- EastPanel

텍스트 필드에 값 누적하기!
이벤트 객체의 getActionCommand() 함수를 이용하여 텍스트 값을 가져왔다
이를 --> 멤버변수 String 객체에 담았다


텍스트값을 가져와서 + 전달받은 멤버변수 문자열을 누적시킨다




4. Total DB 테이블 생성

정산용 DB테이블
판매가 진행되면 insert로 값을 입력한다
값은? ==> shop.getPrice()

이 표를 보여주는 창을 띄우고
값을 다 더해서 보여주자
totalModel 출력하고 getValuAt 으로 더해주자!


** 산수의 두 가지 방법!









